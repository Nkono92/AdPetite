RewriteEngine On

# Laisser passer les fichiers et dossiers existants (CSS, images, etc.)
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Sinon, tout rediriger vers index.php
RewriteRule ^ index.php [L]